name: ble_ftms_demo
description: "EQI 蓝牙健身设备控制 Demo"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_blue_plus: ^1.28.1
  permission_handler: ^11.3.0
  eqi_ble_sdk:
    git:
      url: https://github.com/luck-sz/ble_sdk.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
